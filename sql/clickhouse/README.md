# ClickHouse SQL

This directory contains analytical serving DDL for the approved PostgreSQL `core` handoff.

ClickHouse is downstream and rebuildable. It must never become the canonical source for corrections, lineage or run state.
