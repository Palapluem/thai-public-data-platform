# Test Fixtures

Fixtures are synthetic and safe to commit. They are not production credentials and do not replace the public baseline workbooks.

`bad_data_quality.json` describes the rows and expected blocking checks used to prove that the quality gate stops downstream publication.
